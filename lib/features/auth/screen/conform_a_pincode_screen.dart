import 'package:flutter/material.dart';

class ConformAPincodeScreen extends StatelessWidget {

  const ConformAPincodeScreen({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}