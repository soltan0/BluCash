import 'package:flutter/material.dart';

class BluetoothScrre extends StatelessWidget {

  const BluetoothScrre({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}