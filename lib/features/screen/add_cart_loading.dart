import 'package:flutter/material.dart';

class AddCartLoading extends StatelessWidget {

  const AddCartLoading({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}