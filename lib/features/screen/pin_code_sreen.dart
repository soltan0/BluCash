import 'package:flutter/material.dart';

class PinCodeSreen extends StatelessWidget {

  const PinCodeSreen({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}